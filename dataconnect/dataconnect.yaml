specVersion: "v1beta"
serviceId: "salty-sol"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "salty-sol-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
